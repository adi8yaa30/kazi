# robots.txt — Kazi Network
# https://kazinetwork.in

User-agent: *
Allow: /
# CSS and JS are intentionally crawlable: Google renders the page
# before ranking it, and blocking them breaks that render.
Disallow: /.claude/

# Common SEO crawlers — allowed, but throttled so they don't
# hammer a media-heavy site.
User-agent: AhrefsBot
Crawl-delay: 10

User-agent: SemrushBot
Crawl-delay: 10

User-agent: MJ12bot
Crawl-delay: 10

Sitemap: https://kazinetwork.in/sitemap.xml
